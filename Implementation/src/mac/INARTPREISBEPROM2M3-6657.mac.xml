<?xml version="1.0" encoding="UTF-8"?>
<Export generator="Cache" version="22" zv="CACHE" ts="2001-01-01 00:00:00">
<Routine name="INARTPREISBEPROM2M3" type="MAC" languagemode="0" timestamp="58440,0"><![CDATA[
INARTPREISBEPROM2M3  ;INARTPREISBEPROM2M3;BEC;KORIGIERTEN PREIS NEU RECHNEN;08.04.03
	;
	;/------------------------------------------------------------------\
	;| Description of Function :
	;|		KORIGIERTEN PREIS NEU RECHNEN
	;|
	;| Inputs : 
	;|
	;|
	;| ByRef :
	;|
	;|
	;| Returns :
	;|
	;|
	;| History :
	;|
	;| BEC	08.04.03
	;|
	;\------------------------------------------------------------------/
	;
	NEW KORREKTUR,DATEN
	SET DATEN=$GET(^WWWDATEN(YM,+$HOROLOG,YUSER,YFORM,"D",1))                       ;BEC;;11.06.03;0 DURCH YM GEÄNDERT
	IF YINHALT'="" DO
	. SET FORMEL=YINHALT
	. SET YPREIS=$PIECE(DATEN,Y,2)
	. DO RECHFORMEL
	. ;IF $F($E(KORREKTUR,1),"%") SET KORREKTUR=$P(DATEN,Y,1)+
	. ;
	. SET YPREIS=$$^WWWTR(0,8,YPREIS)
	. SET %TXT(1)=%TXT(1)_"#YINARTPREISBEPROM2D4~"_YPREIS
	. QUIT
	IF YINHALT="" SET %TXT(1)=%TXT(1)_"#YINARTPREISBEPROM2D4~"_""
	QUIT 
	
RECHFORMEL ;FORMEL AUSRECHNEN ;formula 
	NEW WERT,AKTION,YI,FORMEL1,WERT1
	SET WERT=0  
	QUIT:FORMEL=""
	IF $FIND(FORMEL,"%") DO
	. FOR YI=1:1:2 DO  QUIT:$PIECE(FORMEL,"%",YI,99)=""
	. . IF YI=1 SET AKTION="%"
	. . IF YI=2 SET AKTION=""
	. . SET FORMEL1=$PIECE(FORMEL,"%",YI)
	. . SET VZ="+" 
	. . SET WERT1=FORMEL1
	. . IF $EXTRACT(FORMEL1,1)="-" SET VZ="-" SET WERT1=$EXTRACT(FORMEL1,2,999)
	. . IF $EXTRACT(FORMEL1,1)="+" SET VZ="+" SET WERT1=$EXTRACT(FORMEL1,2,999)
	. . IF AKTION="" SET WERT1=$$^WWWTR(1,8,FORMEL1)
	. . IF AKTION'="" DO
	. . . IF AKTION="%" SET WERT1=($TRANSLATE(WERT1,",",".")/100)*YPREIS
	. . . IF VZ="+" SET WERT1=YPREIS+WERT1
	. . . IF VZ="-" SET WERT1=YPREIS-WERT1
	. . . QUIT
	. . SET WERT=WERT+WERT1
	. . QUIT
	IF '$FIND(FORMEL,"%") IF FORMEL'="" SET WERT=$$^WWWTR(1,8,FORMEL)
	IF '$FIND(FORMEL,"%") IF FORMEL'="" IF $FIND(FORMEL,"+") SET WERT=YPREIS+$$^WWWTR(1,8,FORMEL)  ;YPREIS AUS WWWDATEN ;out of 
	IF '$FIND(FORMEL,"%") IF FORMEL'="" IF $FIND(FORMEL,"-") SET WERT=YPREIS+$$^WWWTR(1,8,FORMEL)  ;YPREIS AUS WWWDATEN ;out of 
	SET YPREIS=WERT
	QUIT
]]></Routine>
</Export>