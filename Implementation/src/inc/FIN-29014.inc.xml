<?xml version="1.0" encoding="UTF-8"?>
<Export generator="Cache" version="22" zv="CACHE" ts="2001-01-01 00:00:00">
<Routine name="FIN" type="INC" timestamp="58440,0"><![CDATA[
	;-------------------------------------------------------------------------------
	;
	; History:
	; 24-Jun-2005	RobertW	SR12690: Removed this as it's causing HTML issues with \'s.
	; 21-Jan-2005	GRF		Standardise DevUser macros in COMSYS
	; 10-Dec-2004	GRF		SR11208 : Move "Storage" definition to COMSYS.INC for
	; 						use by other modules
	; 12-Nov-2004	GRF		Remove entries already defined in COMSYS.INC
	; 24-May-2004	SCR		Added Foreign Currency Macros
	; 13-May-2004	GRF		ALWAYS/NEVER for If test substitution
	;-------------------------------------------------------------------------------
	
#include COMSYS
 
#;def1arg Text(%str)		$$DecodeError^FINUtils(%str)
	; TODO : Check FINUtils is the same as COMUtils and if so remove
	;        since already in COMSYS.INC
	; NOTE : Not the same - FINUtils is a "wrapper" for COMUtils - question then
	;        becomes why isn't extra code needed for other calling routines?
 
]]></Routine>
</Export>